import DetailLayoutIntern from 'app/SBE/detail-profile/detail-layout/intern'

const Intern = ({ sbeCode, targetCode }) => {
  return <DetailLayoutIntern sbeCode={sbeCode} targetCode={targetCode} />
}

export default Intern
