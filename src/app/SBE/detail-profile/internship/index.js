import DetailLayoutInternShip from 'app/SBE/detail-profile/detail-layout/internship'

const Internship = ({ sbeCode, targetCode }) => {
  return <DetailLayoutInternShip sbeCode={sbeCode} targetCode={targetCode} />
}

export default Internship
