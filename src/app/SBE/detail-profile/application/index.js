import DetailLayoutApplication from 'app/SBE/detail-profile/detail-layout/application'

const Application = ({ sbeCode, targetCode }) => {
  return <DetailLayoutApplication sbeCode={sbeCode} targetCode={targetCode} />
}

export default Application
