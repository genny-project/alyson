import { useSelector } from 'react-redux'
import { Card, CardHeader } from '@material-ui/core'

const Internmatch = ({ parentCode, code, columns }) => {
  return <Card></Card>
}

export default Internmatch
