import { Box } from '@chakra-ui/react'

const SBEAddBoolean = ({ askData, value, onChange, disabled }) => {
  const questionCode = askData?.questionCode || ''

  return <Box />
}

export default SBEAddBoolean
